# Webpack + TailwindCSS

This is a basic setup of webpack and TailwindCSS.

### Get started

With Node installed, run:

```shell
npm install
npm start

# or, to build for production:
npm run build
```
